# ATM-SIMULATOR
A Java-based ATM simulator that implements secure PIN and OTP authentication, supports deposit, withdrawal, fund transfer, and transaction history. It enforces denomination rules (₹100/200/500), uses error handling for safe operations, and optionally integrates a database for realistic banking simulation.
